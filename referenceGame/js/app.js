//console.log('connected_the_game_function.js');
/*
$( () => {
//get the game ready
    the_game = () => {
    
    requestAnimationFrame(the_game);//execute repeatpass the function the_game
    console.log('123');
    //cancelAnimationFrame(bubble_id); this will pause the animation frame
}
    const $body = $('body');
$('html').on('keydown', (event) => {
    const $bubble = () => {
    if(event.key == " ") { //The space key, space bar
        $bubble = $('<div>');
        $bubble.addClass('bubble');
        $bubble.css({
            //constrain the random coordinates to the dimensions of the window.
            'top': Math.random() * (document.documentElement.clientHight - 60),
            'left': Math.random() * (document.documentElement.clientWidth - 50)
        });
        $body.append($bubble);
    }
    console.log('123');
};
})
requestAnimationFrame(the_game);
//make the bubble explode when clicked
$body.on('click', '.bubble', () => {
//new is-popping class will change the width and height to 0, shrinking opacity to 0 to fade
    $(this).addClass('is-popping');
})
$body.on('transitionend', 'bubble', () => {
    $(this).remove();
}
*/


